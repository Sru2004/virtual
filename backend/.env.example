# =============================================================================
# MongoDB Configuration (REQUIRED for deployment)
# =============================================================================

# Option 1: MongoDB Atlas (Cloud) - RECOMMENDED for deployment
# Get your connection string from: https://www.mongodb.com/cloud/atlas
# 1. Create a free cluster
# 2. Go to "Connect" → "Connect your application"
# 3. Copy the connection string
# 4. Replace <username> and <password> with your Atlas credentials
# Example: mongodb+srv://myuser:mypassword@cluster0.xyz.mongodb.net/?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-name>.mongodb.net/?retryWrites=true&w=majority

# Option 2: Local MongoDB (for development only)
# Install MongoDB from: https://www.mongodb.com/try/download/community
# Make sure MongoDB is running on localhost:27017
# MONGODB_URI=mongodb://localhost:27017/test

# =============================================================================
# Server Configuration
# =============================================================================

# Server port (default: 5000)
PORT=5000

# Frontend URL for CORS (optional - for production)
# FRONTEND_URL=https://your-frontend.vercel.app

# Node environment
NODE_ENV=development

# =============================================================================
# Security
# =============================================================================

# JWT secret for token signing (IMPORTANT: Change this in production!)
# Use a strong random string for production
JWT_SECRET=your_super_secret_jwt_key_change_in_production

# =============================================================================
# Image Upload Configuration (optional)
# =============================================================================

# Maximum image file size in bytes (default: 5242880 = 5MB)
# MAX_IMAGE_BYTES=5242880

# Image similarity threshold (default: 8)
# IMAGE_SIMILARITY_THRESHOLD=8
