# Option A - Local MongoDB (no Atlas needed): install MongoDB, run it, then use:
# MONGODB_URI=mongodb://localhost:27017/visualart

# Option B - MongoDB Atlas: set your connection string and whitelist your IP in Atlas → Network Access
# MONGODB_URI=mongodb+srv://user:pass@cluster0.xxxxx.mongodb.net/?appName=Cluster0

JWT_SECRET=your_jwt_secret_here
PORT=5000
NODE_ENV=development
