# MongoDB Atlas (cloud) — database "test" holds: users, addresses, wishlists, artworks, orders, reviews, artistprofiles
# 1. Create cluster at https://cloud.mongodb.com → Connect → Drivers → copy Node.js connection string
# 2. Put database name in path: ...mongodb.net/test?...
# 3. Whitelist your IP in Atlas → Network Access (e.g. add 157.32.122.224/32, or 0.0.0.0/0 for testing)
# MONGODB_URI=mongodb+srv://USER:PASSWORD@cluster0.xxxxx.mongodb.net/test?retryWrites=true&w=majority

# Local fallback (if MONGODB_URI not set): localhost:27017/test
# MONGODB_URI=mongodb://localhost:27017/test

JWT_SECRET=your_jwt_secret_here
PORT=5000
NODE_ENV=development
