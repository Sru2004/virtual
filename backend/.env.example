MONGODB_URI=mongodb+srv://truptikandalkar0:Radha3992@cluster0.low5vpa.mongodb.net/?appName=Cluster0
JWT_SECRET=your_jwt_secret_here
PORT=5000
NODE_ENV=development

# For production use, set MONGODB_URI to your Atlas connection string and
# keep `JWT_SECRET` secure (do NOT commit real secrets to the repository).
